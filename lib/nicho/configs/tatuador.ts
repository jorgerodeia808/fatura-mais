import type { NichoConfig } from '../types'

// Placeholder — a adaptar quando o nicho for lançado
const tatuador: NichoConfig = {
  id: 'tatuador',
  nome: 'Tattoo+',
  nomeProfissional: 'tatuador',
  nomeNegocio: 'estúdio',
  nomePlural: 'tatuadores',
  cor: '#1a1a2e',
  subdominio: 'tatuador',
  features: {
    marcacoes: true,
    despesas: true,
    relatorios: true,
    conselheiro_ia: true,
    produtos: false,
    faturacao: true,
  },
}

export default tatuador
